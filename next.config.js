/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "https://digitalhippo.onrender.com/"
        ]
    }
}

module.exports = nextConfig
